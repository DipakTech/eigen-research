# Eigen Research website

## Run locally

Open this folder in VS Code, then:

```bash
nvm use 24
npm install
npm run dev
```

Open:

http://localhost:3000

Before publishing, open `app/page.tsx` and replace:

`REPLACE_WITH_YOUR_EMAIL`

with your actual email.
# eigen-research
